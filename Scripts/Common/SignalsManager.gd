extends Node

# Declare member variables here. Examples:
signal _s_broDamaged

signal _s_boyDamaged

signal _s_toyRepaired

signal _s_monsterEnded

signal _s_toyEnded
#SIGNAL FOR HERO DIRECTIONS : ROTATION ANGLE
signal _s_broDirection(dir)

#signal _s_menuBtn(btn)
# Called when the node enters the scene tree for the first time.
func _ready():
	pass # Replace with function body.

# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass
